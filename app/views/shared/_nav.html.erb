<nav class="bg-white shadow-sm border-b border-gray-200 fixed top-0 left-0 right-0 z-50">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <%# Left side - App name/logo %>
      <div class="flex items-center">
        <%= link_to root_path, class: "flex items-center space-x-2" do %>
          <span class="text-2xl font-bold text-gray-900">Meal Composer</span>
        <% end %>
      </div>

      <%# Center - Main navigation (when you add more pages) %>
      <div class="hidden md:flex items-center space-x-6">
        <% if authenticated? %>
          <%# Add your main navigation links here later %>
          <%#= link_to "Meals", meals_path, class: "text-gray-700 hover:text-gray-900 font-medium" %>
          <%#= link_to "Recipes", recipes_path, class: "text-gray-700 hover:text-gray-900 font-medium" %>
        <% end %>
      </div>

      <%# Right side - Auth links %>
      <div class="flex items-center space-x-4">
        <% if authenticated? %>
          <span class="text-sm text-gray-600 hidden sm:inline">
            <%= Current.user.email_address %>
          </span>
          <%= button_to "Sign Out",
                        session_path,
                        method: :delete,
                        class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors",
                        data: { turbo_confirm: "Are you sure you want to sign out?" } %>
        <% else %>
          <%= link_to "Sign In",
                      new_session_path,
                      class: "text-gray-700 hover:text-gray-900 font-medium px-3 py-2" %>
          <%= link_to "Sign Up",
                      new_registration_path,
                      class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-colors" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
